import { View } from "react-native";
import { Colors, Sizes } from "../../constants/styles";

const Divider = () => {
    return (
        <View style={{ backgroundColor: Colors.lightGrayColor, height: 1.0, marginTop: 10.0 , marginBottom: 30.0  }} />
    )
}

export default Divider;